<div class="navbar navbar-default" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
		</div>
		<div class="collapse navbar-collapse" id="bs-navbar">
			<ul class="nav navbar-nav">
				<li class="{{ iif currentURL '/home' 'active' }}"><a href="/home">Home</a></li>
				<li class="{{ iif currentURL '/bylaws' 'active' }}"><a href="/bylaws">Bylaws</a></li>
			</ul>
		</div>
	</div>
</div>
<div class="jumbotron">
	<div class="container">
		<h1>{{ siteTitle }}</h1>
	</div>
</div>
<div class="container">
	<div class="row">
		<div class="col-md-12">
			<h2>{{{ pageContent.title }}}</h2>
			{{#if pageContent.splash}}
				<img src="{{ pageContent.splash.fields.file.url }}" alt="{{ pageContent.splash.fields.title }}" class="pull-right" style="padding: 0 0 10px 10px">
			{{/if}}
			{{{ md pageContent.body }}}
		</div>
	</div>
	<hr>
	<footer>
		<p>&copy; {{ now "YYYY" }} {{ siteTitle }}</p>
	</footer>
</div>
